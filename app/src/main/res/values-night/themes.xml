<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Bridge theme: AppCompat base so AAPT resolves, Compose handles all real theming -->
    <style name="Theme.SXamTop" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>